SELECT DISTINCT
LNP_RECORD_KEY,
REGEXP_SUBSTR( EXTN_ATTRIBUTE_CHAR035,'[^,]+', 1, LEVEL) SERVICE_TYPE_CODE
FROM PSC_LNP_RECORD
WHERE LNP_RECORD_ID = :P_LNP_RECORD_ID
CONNECT BY LEVEL <= REGEXP_COUNT(EXTN_ATTRIBUTE_CHAR035, ',') + 1
