SELECT 
LNP_RECORD_KEY,

CASE 
WHEN ORGANIZATION_NAME IS NOT NULL THEN CONCAT('and ', ORGANIZATION_NAME)
ELSE CONCAT ('and ', PERSON_NAME)
END AS ADDITIONAL_CONTACT_NAME

FROM PSC_LNP_CONTACT_LIST
WHERE PRIMARY_FLAG ='N'
