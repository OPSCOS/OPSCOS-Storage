SELECT 
LNP_RECORD_KEY,
PARCEL_KEY,
ADDRESS1 || ' ' ||  CITY || ' , '|| STATE || ' ' || POSTAL_CODE AS PARC_ADDRESS

FROM PSC_LNP_PARCEL_ADDRESS
WHERE PRIMARY_FLAG = 'Y'
AND PRIMARY_TRANS_ADDRESS = 'Y'
